// Legacy import path kept for compatibility. DB access lives in inventory infrastructure.
export { DailyInventoryRepository } from "@/modules/inventory/infrastructure/repositories/DailyInventoryRepository"
