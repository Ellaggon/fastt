// Legacy import path kept for compatibility. DB access lives in inventory infrastructure.
export { InventoryBootstrapService } from "@/modules/inventory/infrastructure/services/InventoryBootstrapService"
