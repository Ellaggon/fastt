---
import {
	canonicalPublicDepartmentSlug,
	publicDestinationHref,
} from "@/lib/marketplace/publicDestinationRoutes"

const canonicalSlug = canonicalPublicDepartmentSlug(Astro.params.dept)
return Astro.redirect(canonicalSlug ? publicDestinationHref(canonicalSlug, "tours") : "/tours", 308)
---
