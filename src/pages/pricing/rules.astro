---
const target = new URL("/rates/multi-calendar", Astro.url)
target.search = Astro.url.search || "?tab=rules"
target.searchParams.set("tab", "rules")
return Astro.redirect(target.toString(), 308)
---
