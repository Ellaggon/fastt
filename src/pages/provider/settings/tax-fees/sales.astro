---
import { routes } from "@/lib/routes"

const target = routes.providerSettingsTaxFees()
const query = Astro.url.searchParams.get("create") === "1" ? "?create=1" : ""

return Astro.redirect(`${target}${query}`)
---
