<style is:global>
	@media (max-width: 767px), (max-height: 480px) and (pointer: coarse) {
		.mobile-calendar-action-sheet:not(.hidden) {
			position: fixed;
			inset-inline: 0.75rem;
			inset-block-start: auto;
			inset-block-end: calc(
				0.75rem + env(safe-area-inset-bottom) + var(--mobile-keyboard-offset, 0px)
			);
			max-height: min(13.5rem, calc(100dvh - 2rem - var(--mobile-keyboard-offset, 0px)));
			overflow-y: auto;
			border-radius: 1rem;
			padding: 0.85rem;
			box-shadow: 0 24px 70px rgb(15 23 42 / 0.25);
			animation: mobile-sheet-enter 160ms ease-out;
			overscroll-behavior: contain;
			transform: translateY(var(--mobile-sheet-drag, 0px)) scale(var(--mobile-sheet-scale, 1));
			transition:
				max-height 220ms cubic-bezier(0.2, 0.8, 0.2, 1),
				transform 220ms cubic-bezier(0.2, 0.8, 0.2, 1),
				box-shadow 220ms ease;
			will-change: transform, max-height;
			z-index: 50;
		}

		.mobile-calendar-action-sheet.mobile-sheet-expanded {
			max-height: min(88dvh, 42rem);
			box-shadow: 0 28px 90px rgb(15 23 42 / 0.32);
		}

		.mobile-calendar-action-sheet.mobile-sheet-dragging {
			transition: none;
		}

		.mobile-calendar-action-sheet.mobile-sheet-settling {
			transition:
				max-height 260ms cubic-bezier(0.16, 1, 0.3, 1),
				transform 260ms cubic-bezier(0.16, 1, 0.3, 1),
				box-shadow 220ms ease;
		}

		.mobile-calendar-sheet-backdrop:not(.hidden) {
			position: fixed;
			inset: 0;
			z-index: 40;
			background: rgb(15 23 42 / 0.16);
			backdrop-filter: blur(1px);
			pointer-events: none;
		}

		.mobile-sheet-handle {
			display: block;
		}

		.mobile-calendar-grid {
			padding-bottom: 7.5rem;
		}

		.mobile-calendar-cell {
			min-height: 7.25rem;
		}

		.calendar-sheet-open {
			scroll-padding-bottom: 11rem;
		}

		@keyframes mobile-sheet-enter {
			from {
				opacity: 0;
				transform: translateY(1rem) scale(0.98);
			}
			to {
				opacity: 1;
				transform: translateY(0) scale(1);
			}
		}
	}
</style>
