---
interface Props {
	class?: string
	ariaLabel?: string
}

const { class: classProp = "", ariaLabel } = Astro.props
---

<section class:list={["grid grid-cols-2 gap-3 sm:grid-cols-3", classProp]} aria-label={ariaLabel}>
	<slot />
</section>
