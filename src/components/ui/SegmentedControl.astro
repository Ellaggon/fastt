---
const { className = "", class: classProp = "", ...props } = Astro.props
const finalClass =
	`fastt-segmented-control fastt-tabs inline-flex gap-1 overflow-x-auto p-1 text-sm font-semibold text-slate-300 ${className} ${classProp}`.trim()
---

<nav class={finalClass} {...props}>
	<slot />
</nav>
