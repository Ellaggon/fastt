---
const { latitude, longitude, address } = Astro.props
---

<div
	data-modal="map"
	class="modal-overlay fixed inset-0 z-30 flex hidden items-center justify-center bg-slate-950/78 backdrop-blur-md transition-opacity duration-300 md:p-4"
>
	<button
		id="modal-close"
		data-close-modal
		class="fastt-button absolute right-0 top-0 z-20 m-4 flex h-10 w-10 items-center justify-center bg-white/12 text-white hover:bg-white/20 md:right-10 md:top-10"
	>
		<svg
			xmlns="http://www.w3.org/2000/svg"
			class="h-5 w-5 text-white"
			viewBox="0 0 24 24"
			fill="none"
			stroke="currentColor"
			stroke-width="3"
			stroke-linecap="round"
			stroke-linejoin="round"
		>
			<line x1="18" y1="6" x2="6" y2="18"></line>
			<line x1="6" y1="6" x2="18" y2="18"></line>
		</svg>
	</button>
	<div
		id="map-container"
		class="fastt-dialog relative h-[70%] w-[96%] max-w-3xl overflow-hidden lg:static"
	>
		<div id="modal-map" class="h-full w-full bg-slate-100"></div>

		<input
			type="text"
			class="hidden"
			id="modal-map-data"
			value={JSON.stringify({ latitude, longitude, address })}
		/>

		<div
			id="modal-map-fallback"
			class="flex hidden h-64 w-full items-center justify-center bg-slate-100"
		>
			No localizada
		</div>
	</div>
	<a
		href={`https://www.google.com/maps?q=${latitude},${longitude}`}
		target="_blank"
		rel="noopener noreferrer"
		class="fastt-button absolute right-[50%] bottom-10 z-20 translate-x-1/2 items-center justify-center border border-white/20 bg-slate-950/78 px-4 py-2 text-sm font-medium text-white backdrop-blur hover:bg-slate-900 md:bottom-20"
	>
		Ver en Google Maps
	</a>
</div>
<script>
	import { registerModal } from "@/lib/utils/modal-manager"

	import * as L from "leaflet"

	let map: L.Map | null = null

	registerModal("map", {
		onOpen(modal) {
			const el = modal.querySelector<HTMLElement>("#modal-map")
			if (!el) return

			if (map) {
				setTimeout(() => {
					map?.invalidateSize()
				}, 50)
				return
			}

			const dataEl = modal.querySelector<HTMLInputElement>("#modal-map-data")
			const data = dataEl ? JSON.parse(dataEl.value || "null") : null

			if (!data?.latitude || !data?.longitude) {
				el.classList.add("hidden")
				modal.querySelector("#modal-map-fallback")?.classList.remove("hidden")
				return
			}

			map = L.map(el, { scrollWheelZoom: false }).setView([data.latitude, data.longitude], 14)

			L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", {
				attribution: "&copy; OpenStreetMap contributors",
			}).addTo(map)

			L.marker([data.latitude, data.longitude]).addTo(map)

			setTimeout(() => {
				map!.invalidateSize()
			}, 50)
		},
	})
</script>
