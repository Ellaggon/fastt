---
const { hotel } = Astro.props
---

<section class="grid gap-2 text-gray-800">
	<p><strong>Estrellas:</strong> {hotel?.stars ?? "—"}</p>
	<p><strong>Dirección:</strong> {hotel?.address ?? "—"}</p>
	<p><strong>Teléfono:</strong> {hotel?.phone ?? "—"}</p>
	<p><strong>Check-in:</strong> {hotel?.checkInTime ?? "—"}</p>
	<p><strong>Check-out:</strong> {hotel?.checkOutTime ?? "—"}</p>
</section>
