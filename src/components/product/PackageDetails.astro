---
const { pkg } = Astro.props
---

<section class="grid gap-2">
	<p>
		<strong>Itinerario:</strong>
		<span class="whitespace-pre-wrap">{pkg?.itinerary ?? "—"}</span>
	</p>
	<p><strong>Días:</strong> {pkg?.days ?? "—"}</p>
	<p><strong>Noches:</strong> {pkg?.nights ?? "—"}</p>
</section>
